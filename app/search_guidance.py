# app/search_guidance.py
"""联网检索的提问方式指引（拼进系统提示词，全局生效）。

起因：用户问「总结 AI 未来 2 年的发展情况」，模型把这句原话整个塞进 web_search 的
query、条数取默认最小值、只搜一次就下笔。这样召回的多是标题党综述和营销稿，且
「未来 2 年」是相对表述——搜索引擎不认，可能召回几年前某篇同样写「未来两年」的旧文。

现有防线拦不住它：app/tools/validating.py 的检索校验只判空命中/低相关，而这条 query
必然返回若干条非空、表面看也相关的结果，必然放行。它防的是「搜不到」，不是「问得烂」。

联网搜索是 MCP 接入的第三方工具（见 mcp/mcp_servers.json），工具描述改不了，故只能
从系统提示词这一侧约束调用方式。

刻意写清「何时不拆」：这段是全局注入的，若一味要求拆查询，「Python 怎么读文件」这类
一次就能查到的问题会被拖成 5 次检索——慢、贵，且无助于答案质量。

「多个搜索来源要均衡使用」：接入多家搜索 MCP（博查 + 百炼）后，模型会固定偏向其中一个
（实测两个都连上、却每轮只调博查）。工具描述来自远程、改不了，LLM 选工具也没有权重档，
只能从系统提示这一侧弱引导——把拆出的子查询分摊到不同工具、鼓励轮换。属尽力而为，不保证
严格 50/50；要严格等权得改成「一个聚合工具扇出到两家、结果各半合并」。
"""
from __future__ import annotations

SEARCH_SYSTEM_GUIDANCE = (
    "\n\n## 联网检索的提问方式\n"
    "调用联网搜索工具时，query 是写给搜索引擎的检索词，不是写给人看的问句。"
    "把用户原话整句照抄进 query 是最常见的错误——那样召回的多是泛泛综述与营销稿，"
    "拿不到有信息量的一手材料。\n"
    "【时间必须锚定】query 里不得出现「今年 / 去年 / 最近 / 最新 / 未来 N 年」这类相对表述，"
    "一律先按系统提示里给出的今天日期换算成绝对年份再写进检索词。搜索引擎不认相对时间：照抄"
    "「未来 2 年」很可能召回若干年前某篇同样写着「未来两年」的旧文，而你无从分辨。\n"
    "【何时拆成多条】用户的问题若横跨多个方面、或宽泛到一条检索词覆盖不了"
    "（「总结 X 的发展」「X 现状如何」「对比 X 和 Y」），先拆成 3-5 条彼此正交的子查询"
    "分别检索，每条只打一个具体侧面，而不是用一条大而空的词去搜。\n"
    "【何时不拆】事实明确、一条检索词就能查到的问题（某 API 怎么用、某事件哪天发生、"
    "某个数字是多少）直接搜一次即可。不要为拆而拆——那只会把简单问题搞慢搞贵。\n"
    "【条数与补搜】宽泛问题别用条数参数的默认最小值，给足；动笔前先自评手上材料"
    "够不够支撑结论：某个侧面一条来源都没有、或几条来源都是同一口径的转述，就再补一轮检索。"
    "不要拿一次搜索的前几条硬凑成总结。\n"
    "【多个搜索来源要均衡使用】系统可能同时接入多个联网搜索工具（如博查、百炼），"
    "它们能力相当、地位平等、不分主次。**不要固定只用其中某一个**：把上面拆出的多条子查询"
    "分摊到不同的搜索工具上；需要交叉印证、或某个工具结果不理想时，换用另一个补搜。"
    "默认在可用的搜索工具之间轮换均衡，而不是每轮都调同一个。"
)
