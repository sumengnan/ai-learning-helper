{
  "servers": [
    {
      "_comment": "阿里云百炼 WebSearch：实时互联网检索。需 HARNESS_ENABLE_MCP=true + 环境变量 DASHSCOPE_API_KEY，缺任一则静默失效。",
      "name": "websearch",
      "enabled": true,
      "transport": "http",
      "url": "https://dashscope.aliyuncs.com/api/v1/mcps/WebSearch/mcp",
      "headers": {"Authorization": "Bearer ${DASHSCOPE_API_KEY}"},
      "timeout": 30
    }
  ]
}
